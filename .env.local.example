# =============================================================
# .env.local — Personal secrets (gitignored, never committed)
# Copy this file: cp .env.local.example .env.local
# =============================================================

# Your name — shows in the banner
CHEF_NAME="Your Name"

# Local database password
DATABASE_PASSWORD="your_local_password"

# Secret recipe
CHEF_SECRET_RECIPE="your secret ingredient"
